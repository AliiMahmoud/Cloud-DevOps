version: 2.1

# # reusable block of code
# commands:
#   command1:
#     description:
#     parameters:
#       workflow_id:
#         type: string
#       event:
#         type: string
#     steps: # - steps   

# 
jobs:
  build:
  # env variables for the job level
    environment:
      CODE: 123
    docker:
      - image: circleci/node
    steps:
      - run:
          name: building first step
          command: |
            echo "Hello, Friend!"

  test:
  # env variables for the job level
    environment:
      CODE: 123
    docker:
      - image: circleci/node
    steps:
      - run:
          name: building first step
          command: |
            echo "testing"


# list of workflows
# each has the order in which job should be running
workflows:
  default:
    jobs:
      - build # ref
      - test
